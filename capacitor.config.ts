import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.watermelonsorter.game',
  appName: 'Watermelon Sorter',
  webDir: 'dist'
};

export default config;
